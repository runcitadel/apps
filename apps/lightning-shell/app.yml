# SPDX-FileCopyrightText: 2021 Citadel and contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# yaml-language-server: $schema=../../app-standard-v1.json

version: 1

metadata:
  category: Lightning Node Management
  name: Lightning Shell
  version: 1.1.1
  tagline: Web shell with a selection of LN node management utilities
  description: >-
    This app allows you to access a lot of terminal apps directly on the web.

    The default username for this app is "citadel".
  developer: Ioan Bizău
  website: Ioan Bizău
  dependencies:
    - lnd
  repo: https://ibz.github.io/lightning-shell/
  support: https://github.com/ibz/lightning-shell/issues
  gallery:
  - 1.jpg
  - 2.jpg
  - 3.jpg
  defaultPassword: $APP_SEED

containers:
  - name: web
    image: ghcr.io/runcitadel/lightning-shell:main@sha256:699428fd5bbe11f951ccb9148a0004a936a0f7de024fe469ce918c7a0166beca
    restart: on-failure
    stop_grace_period: 1m
    port: 7681
    data:
    - data:/data
    permissions:
      - lnd
    environment:
      APP_PASSWORD: "${APP_SEED}"
      USERNAME: citadel
      LND_IP: "${LND_IP}"
      LND_GRPC_PORT: ${LND_GRPC_PORT}
